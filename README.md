#Websockets Front-End

https://websockets.readthedocs.io/en/stable/intro/tutorial2.html#solution

To start a local server:

```
python -m http.server
```

Then open http://localhost:8000 in your browser. You should see a Connect 4 game.
Join the game in a different browser http://localhost:8000/?join={join_code}
